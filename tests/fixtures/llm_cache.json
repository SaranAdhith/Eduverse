{
  "_note": "DOC_09 §6 recorded LLM cache. The e2e suite uses a deterministic synthesizer (tests/e2e/conftest.py MockAnthropic) rather than replaying recorded prompts, so this file is intentionally empty. `pytest --record` (human-run, never in CI) would populate it against the real API if prompt replay is ever preferred over synthesis."
}
