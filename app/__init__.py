"""Eduverse FastAPI backend service (DOC_01 scaffold).

The DOC_00 data foundation lives in the installable ``eduverse`` package under
``src/``. This ``app`` package is the runnable web service (``uvicorn
app.main:app``) that later phases build their endpoints on top of.
"""
