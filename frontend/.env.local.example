# Backend API base URL (DOC_07 §5).
NEXT_PUBLIC_API_URL=http://localhost:8000

# Study-condition assignment is DOC_08's job; until then the frontend creates a
# path with this mode. Never surfaced to participants (DOC_07 §8.3). TODO_ASSIGNMENT.
NEXT_PUBLIC_DEFAULT_MODE=personalized
NEXT_PUBLIC_DEFAULT_BLOCK=A
